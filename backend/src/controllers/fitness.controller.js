const workoutsController = async (_req, res) => {
  res.status(501).json({ error: "Not wired yet" });
};
const activityDailyController = async (_req, res) => {
  res.status(501).json({ error: "Not wired yet" });
};
const attendanceController = async (_req, res) => {
  res.status(501).json({ error: "Not wired yet" });
};
const goalsController = async (_req, res) => {
  res.status(501).json({ error: "Not wired yet" });
};
const streaksController = async (_req, res) => {
  res.status(501).json({ error: "Not wired yet" });
};
export {
  activityDailyController,
  attendanceController,
  goalsController,
  streaksController,
  workoutsController
};
