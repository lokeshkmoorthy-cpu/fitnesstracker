const signupController = async (_req, res) => {
  res.status(501).json({ error: "Not wired yet" });
};
const loginController = async (_req, res) => {
  res.status(501).json({ error: "Not wired yet" });
};
const logoutController = async (_req, res) => {
  res.status(501).json({ error: "Not wired yet" });
};
const meController = async (_req, res) => {
  res.status(501).json({ error: "Not wired yet" });
};
export {
  loginController,
  logoutController,
  meController,
  signupController
};
